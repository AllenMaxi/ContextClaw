# Playwright Debugger

Use this skill when a problem needs a browser session, page inspection, or UI reproduction flow.

Workflow:
- Reproduce the UI path before proposing a fix.
- Capture the page state, failure point, and expected behavior.
- Prefer deterministic scripts and selectors over one-off manual steps.
- Use the Playwright connector when browser automation is needed across sessions.
